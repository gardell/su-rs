# su-rs

## Usage
### Build
```sh
# `debug` mode
./build.sh
# `release` mode
TARGET=release ./build.sh
```

### Run
```sh
# `debug` artifact
target/debug/su-rs
# `release` artifact
target/release/su-rs
```
